include ../../atoms/icon/icon

mixin add-advert(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div.m-add-advert(class=spec.__class)&attributes(attributes)
    .m-add-advert__text Додати оголошення
    .m-add-advert__btn
      +icon({ '__class': 'm-add-advert__icon', icon: 'plus' })
