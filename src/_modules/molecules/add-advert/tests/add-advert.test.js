'use strict';

import AddAdvert from '../add-advert';

describe('AddAdvert View', function() {

  beforeEach(() => {
    this.addAdvert = new AddAdvert();
  });

  it('Should run a few assertions', () => {
    expect(this.addAdvert);
  });

});
