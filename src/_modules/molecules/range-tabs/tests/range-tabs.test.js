'use strict';

import RangeTabs from '../range-tabs';

describe('RangeTabs View', function() {

  beforeEach(() => {
    this.rangeTabs = new RangeTabs();
  });

  it('Should run a few assertions', () => {
    expect(this.rangeTabs);
  });

});
