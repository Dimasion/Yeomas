'use strict';

import Sort from '../sort';

describe('Sort View', function() {

  beforeEach(() => {
    this.sort = new Sort();
  });

  it('Should run a few assertions', () => {
    expect(this.sort);
  });

});
