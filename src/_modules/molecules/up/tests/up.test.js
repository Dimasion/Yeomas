'use strict';

import Up from '../up';

describe('Up View', function() {

  beforeEach(() => {
    this.up = new Up();
  });

  it('Should run a few assertions', () => {
    expect(this.up);
  });

});
