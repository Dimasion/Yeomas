include ../../molecules/menu/menu
include ../../molecules/search/search
include ../../molecules/add-advert/add-advert

mixin header(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''
  - spec.currentPage = spec.currentPage || 'home'

  div(class=spec.__class).o-header&attributes(attributes)
    .container
      .o-header__content
        +menu({ '__class': 'o-header__menu', active: spec.currentPage })
        +search({ '__class': 'o-header__search' })
        +add-advert({ '__class': 'o-header__advert' })
