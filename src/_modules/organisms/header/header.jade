include ../../molecules/menu/menu

mixin header(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''
  - spec.currentPage = spec.currentPage || 'home'

  div(class=spec.__class).header&attributes(attributes)
    .container
      +menu({ '__class': 'header__menu', active: spec.currentPage })
