'use strict';

import Ibutton from '../ibutton';

describe('Ibutton View', function() {

  beforeEach(() => {
    this.ibutton = new Ibutton();
  });

  it('Should run a few assertions', () => {
    expect(this.ibutton);
  });

});
