'use strict';

import Range from '../range';

describe('Range View', function() {

  beforeEach(() => {
    this.range = new Range();
  });

  it('Should run a few assertions', () => {
    expect(this.range);
  });

});
